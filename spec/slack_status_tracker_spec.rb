require 'spec_helper'

describe SlackStatusTracker do
  it 'has a version number' do
    expect(SlackStatusTracker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
